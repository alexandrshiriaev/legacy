<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Поездки сотрудника</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<section class="container">
    <a href="/" class="nav-button">Вернуться обратно</a>
    <h1>Список командировок {{name}} </h1>
    {{#if trips.length}}
        <ul class="list">
            {{#each trips}}
                <a class="list__el" href="/trips/{{this.id}}">{{this.datestart}} - {{this.dateend}}</a>
            {{/each}}
        </ul>
    {{/if}}
    {{#unless trips.length}}
        <h3>Список командировок пуст.</h3>
    {{/unless}}
    <button id="add-trip" onclick="openModal" class="button add-button">Добавить командировку</button>
</section>
<div id="modal" style="display: none;">
    <div id="modal-content">
        <h2>
            Добавить новую поездку
        </h2>
        <form id="add-trip-form" action="/trips" method="post">
            <div class="form-group">
                <label for="workerId" class="hidden"></label>
                <input type="number" id="workerId" value={{workerId}} name="workerId" class="hidden">
                <div>
                    <label for="dateStart">Дата начала поездки:</label>
                    <input type="date" id="dateStart" name="dateStart" required>
                </div>
                <div>
                    <label for="dateEnd">Дата конца поездки:</label>
                    <input type="date" id="dateEnd" name="dateEnd" required>
                </div>
            </div>
            <div class="button-group">
                <button type="submit" class="button">Добавить</button>
                <button type="button" class="button cancel-button" id="modal-close" onclick="closeModal">Отмена</button>
            </div>
        </form>
    </div>
</div>
<script src="/trip.js"></script>
</body>
</html>
